class Service:
    def __init__(self, service_id, service_name, max_service_size, inserted_in):
        #initialize service
        self.service_id = service_id
        self.service_name = service_name
        self.max_service_size = max_service_size
        self.inserted_in = inserted_in